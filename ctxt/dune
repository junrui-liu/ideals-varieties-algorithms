(env
 (dev
  (flags
   (:standard -warn-error -A))))

; (library
;  (name ctxt)
;  (modules ctxt)
;  (preprocess
;   (pps ppx_sexp_conv ppx_deriving.show ppx_deriving.eq ppx_deriving.ord))
;  (libraries core fmt))
